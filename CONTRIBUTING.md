# 贡献指南

感谢你对 SysPulse 的关注！我们欢迎各种形式的贡献。

## 如何贡献

### 报告 Bug

如果你发现了 bug，请创建一个 Issue，包含：

1. **清晰的标题**：简短描述问题
2. **复现步骤**：详细说明如何触发 bug
3. **预期行为**：你期望发生什么
4. **实际行为**：实际发生了什么
5. **环境信息**：
   - 操作系统和版本
   - Go 版本
   - Docker 版本（如果相关）
   - SysPulse 版本

### 提出新功能

如果你有新功能的想法：

1. 先检查 Issues 是否已有类似建议
2. 创建一个 Feature Request Issue
3. 详细描述功能和使用场景
4. 如果可能，提供示例或截图

### 提交代码

1. **Fork 仓库**
   ```bash
   # 克隆你的 fork
   git clone https://github.com/YOUR_USERNAME/syspulse.git
   cd syspulse
   ```

2. **创建分支**
   ```bash
   git checkout -b feature/your-feature-name
   # 或
   git checkout -b fix/your-bug-fix
   ```

3. **开发**
   - 遵循 Go 代码规范
   - 添加必要的注释
   - 确保代码可以编译通过
   - 运行 `go fmt` 格式化代码

4. **测试**
   ```bash
   # 构建
   make build
   
   # 运行
   ./syspulse
   
   # 测试各个命令
   ./syspulse cpu
   ./syspulse memory
   ./syspulse docker
   ```

5. **提交**
   ```bash
   git add .
   git commit -m "feat: 添加新功能说明"
   # 或
   git commit -m "fix: 修复某个问题"
   ```

   提交信息格式：
   - `feat:` 新功能
   - `fix:` Bug 修复
   - `docs:` 文档更新
   - `style:` 代码格式（不影响代码运行）
   - `refactor:` 重构
   - `test:` 测试相关
   - `chore:` 构建过程或辅助工具的变动

6. **推送并创建 Pull Request**
   ```bash
   git push origin feature/your-feature-name
   ```
   
   然后在 GitHub 上创建 Pull Request。

## 代码规范

### Go 代码风格

- 遵循 [Effective Go](https://golang.org/doc/effective_go) 指南
- 使用 `go fmt` 格式化代码
- 使用有意义的变量和函数名
- 添加必要的注释，特别是导出的函数和类型

### 项目结构

```
syspulse/
├── cmd/              # CLI 命令定义
├── internal/         # 内部包
│   ├── monitor/     # 监控逻辑
│   └── display/     # 显示逻辑
├── examples/        # 示例和文档
├── main.go          # 入口文件
├── go.mod           # 依赖管理
└── README.md        # 项目说明
```

### 添加新命令

1. 在 `cmd/` 目录创建新文件，例如 `cmd/newfeature.go`
2. 定义 cobra 命令
3. 在 `cmd/root.go` 中注册命令
4. 在 `internal/monitor/` 添加数据获取逻辑
5. 在 `internal/display/` 添加显示逻辑

示例：

```go
// cmd/newfeature.go
package cmd

import (
	"syspulse/internal/display"
	"syspulse/internal/monitor"
	"github.com/spf13/cobra"
)

var newFeatureCmd = &cobra.Command{
	Use:   "newfeature",
	Short: "新功能描述",
	Run: func(cmd *cobra.Command, args []string) {
		display.Clear()
		display.PrintHeader("🎯 新功能")
		
		data := monitor.GetNewFeatureData()
		display.PrintNewFeatureData(data)
	},
}

func init() {
	// 在 root.go 中调用 rootCmd.AddCommand(newFeatureCmd)
}
```

### 添加新的监控指标

1. 在 `internal/monitor/types.go` 定义数据结构
2. 在 `internal/monitor/` 创建获取函数
3. 在 `internal/display/` 创建显示函数

## 测试

目前项目还没有完整的测试覆盖，这是一个可以贡献的好地方！

添加测试：
1. 在对应包目录创建 `*_test.go` 文件
2. 编写测试函数
3. 运行 `go test ./...`

## 文档

文档同样重要！你可以：

- 改进 README
- 添加使用示例
- 翻译文档到其他语言
- 添加截图和 GIF 演示

## 需要帮助的领域

我们特别欢迎以下方面的贡献：

- 📊 添加更多可视化效果（图表、仪表盘）
- 🧪 添加单元测试和集成测试
- 📝 改进文档和使用示例
- 🌐 国际化支持（i18n）
- 🐛 修复已知的 bug
- ⚡ 性能优化
- 🎨 改进 UI/UX
- 🔌 添加插件系统
- 📡 添加数据导出功能（JSON、CSV）
- 🚨 添加告警系统

## 开发环境设置

```bash
# 克隆仓库
git clone https://github.com/YOUR_USERNAME/syspulse.git
cd syspulse

# 安装依赖
go mod download

# 开发时运行
go run main.go

# 或使用 Makefile
make run

# 构建
make build

# 格式化代码
make fmt
```

## 提问和讨论

- 使用 GitHub Issues 报告问题
- 使用 GitHub Discussions 进行讨论
- 查看现有的 Issues 看看能否帮忙解决

## 行为准则

- 尊重所有贡献者
- 提供建设性的反馈
- 专注于问题本身，不要进行人身攻击
- 欢迎新手，耐心回答问题

## 许可证

通过贡献代码，你同意你的贡献将按照项目的 MIT 许可证进行授权。

---

再次感谢你的贡献！🎉

